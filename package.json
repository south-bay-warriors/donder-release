{
  "name": "donder-release",
  "version": "1.0.0",
  "description": "Quickly create releases on Github from the command line or CI using conventional commits.",
  "repository": {
    "type": "git",
    "url": "https://github.com/cloudoki/donder-release"
  },
  "bin": "./npm/run.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "node ./npm/install.js"
  },
  "author": "Bruno Morgado <bruno@cloudoki.com>",
  "license": "MIT",
  "dependencies": {
    "binary-install": "1.0.6",
    "console.table": "0.10.0"
  },
  "files": [
    "npm/**/*"
  ]
}
